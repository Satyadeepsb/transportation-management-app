# Docker Environment Variables Example
# Copy this file to .env.docker and update with your values

# ============================================
# PostgreSQL Configuration
# ============================================
POSTGRES_USER=tms_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=tms_database

# ============================================
# Backend Configuration
# ============================================
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# JWT Secret - Generate a strong random string
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
JWT_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=production

# ============================================
# Frontend Configuration
# ============================================
VITE_API_URL=http://localhost:3000

# ============================================
# Production Overrides (Optional)
# ============================================
# Use these when deploying to cloud
# DATABASE_URL=postgresql://user:pass@your-cloud-db.com:5432/dbname
# JWT_SECRET=production-secret-from-secrets-manager
